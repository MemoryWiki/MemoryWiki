# Example Memory

This is a tiny public sample memory root. It is safe to delete.

- MemoryWiki stores memory as Markdown and JSONL on local disk.
- Retrieval indexes are rebuildable sidecars, not canonical memory.
